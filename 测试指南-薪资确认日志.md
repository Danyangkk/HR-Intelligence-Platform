# 测试指南 - 薪资确认诊断日志

## 按照要求已完成

✅ **后端加日志定位**：在 `/ask` 接口、`stream` 启动、`planner reject` 三层添加详细打印  
✅ **前端循环保护修复**：使用全局计数器 `AI.payrollConfirmRetry`，最多尝试 2 次后停止  
✅ **服务已重启**：API 和 Nginx 已重启，所有修改已生效

## 快速测试步骤

### 1. 打开后端日志监控

在终端运行（保持窗口开启）：

```bash
docker logs -f hr-agent-api-1 2>&1 | grep -E "\[POST /ask 接口\]|\[Agent Stream 启动\]|\[Planner Reject\]|\[薪资Token验证\]|\[Planner\]"
```

### 2. 清除浏览器缓存和计数器

在浏览器控制台（F12）运行：

```javascript
localStorage.clear();
AI.payrollConfirmRetry = 0;
location.reload();
```

### 3. 复现问题

1. 使用 `biz_hrd` 账号登录
2. 在智能体输入：`我的工资是多少`
3. 填写事由并确认
4. **观察前端和后端的输出**

### 4. 收集日志

#### 前端控制台输出（F12 → Console）

请复制以下关键日志：
- `[reject事件] 需要二次确认，全局重试次数: X`
- `[智能体薪资] ...`
- `[循环保护] ...`（如果触发）

#### 后端日志输出（终端）

请复制以下关键段落：
- `[POST /ask 接口]` 开头的完整段落
- `[Agent Stream 启动]` 开头的完整段落
- `[Planner Reject]` 开头的完整段落（如果有）
- `[薪资Token验证]` 开头的完整段落
- `[Planner]` 开头的完整段落

## 循环保护说明

前端已修复循环保护机制，现在：
- **最多尝试 2 次**（首次 + 1 次重试）
- **第 3 次 reject 会停止**并显示错误："后端Token验证异常，已累计 X 次确认请求。请查看后端日志定位问题。"
- 不会再出现 55 次的情况

## 日志解读

后端日志会清楚显示：
1. **请求头是否带上 token**：`[POST /ask 接口] user.payroll_confirmed=True/False`
2. **Token 验证过程**：`[薪资Token验证] ✅/❌ ...`
3. **最终走了哪个分支**：`[Planner Reject] 🚨 走了'需要二次确认'分支` 或 `[Planner] ✅ 放行查询`

---

**完成后，请把前端控制台和后端日志的输出贴给我，我会根据日志定位根因并修复。**
