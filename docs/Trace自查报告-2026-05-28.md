# Agent Harness Trace 表自查报告

**日期**: 2026-05-28  
**自查人**: AI Agent  
**版本**: V2

---

## 1. 表结构检查

### 1.1 `agent_run` 表

✅ **表已存在，字段完整**

| 字段名 | 类型 | 是否必需 | 当前状态 | 说明 |
|--------|------|----------|----------|------|
| `id` | uuid | PK | ✅ 存在 | 运行ID，主键 |
| `session_id` | varchar(64) | 是 | ✅ 存在 | 会话ID，已建索引 |
| `role` | varchar(32) | 是 | ✅ 存在 | 用户角色 |
| `question_hash` | varchar(64) | 是 | ✅ 存在 | 问题SHA-256哈希 |
| `intent` | varchar(32) | 是 | ✅ 存在 | 意图分类 |
| `outcome` | varchar(16) | 是 | ✅ 存在 | 结果: success/reject/clarify/timeout/error |
| `reject_reason` | text | 可空 | ✅ 存在 | 拒绝原因 |
| `replan_count` | integer | 是 | ✅ 存在 | 重新规划次数 |
| `node_count` | integer | 是 | ✅ 存在 | 节点执行次数 |
| `total_ms` | integer | 是 | ✅ 存在 | 总耗时(毫秒) |
| `created_at` | timestamptz | 是 | ✅ 存在 | 创建时间 |
| **反馈采集字段** | | | | |
| `user_feedback` | varchar(8) | 可空 | ✅ 存在 | 用户反馈: up/down |
| `auto_badcase` | boolean | 是 | ✅ 存在 | 自动badcase标记，已建索引 |
| `badcase_reason` | varchar(64) | 可空 | ✅ 存在 | badcase原因 |
| `review_status` | varchar(16) | 是 | ✅ 存在 | 复盘状态: pending/reviewed/fixed/ignored，已建索引 |

**索引情况**:
- ✅ `ix_agent_run_session_id` (session_id)
- ✅ `ix_agent_run_auto_badcase` (auto_badcase)
- ✅ `ix_agent_run_review_status` (review_status)

---

### 1.2 `agent_node_trace` 表

✅ **表已存在，字段完整**

| 字段名 | 类型 | 是否必需 | 当前状态 | 说明 |
|--------|------|----------|----------|------|
| `id` | bigserial | PK | ✅ 存在 | 自增主键 |
| `run_id` | uuid | FK | ✅ 存在 | 关联agent_run，ON DELETE CASCADE，已建索引 |
| `seq` | integer | 是 | ✅ 存在 | 节点执行序号 |
| `node` | varchar(32) | 是 | ✅ 存在 | 节点类型 |
| `agent` | varchar(32) | 是 | ✅ 存在 | Agent名称 |
| `skills_loaded` | jsonb | 可空 | ✅ 存在 | 加载的skill元信息 |
| `tools_called` | jsonb | 可空 | ✅ 存在 | 调用的tool名称+耗时 |
| `status` | varchar(16) | 是 | ✅ 存在 | 状态: ok/retry/failed/timeout/skipped |
| `attempt` | integer | 是 | ✅ 存在 | 尝试次数 |
| `duration_ms` | integer | 是 | ✅ 存在 | 执行耗时(毫秒) |
| `decision` | jsonb | 可空 | ✅ 存在 | 判定元信息（最关键） |
| `error_type` | varchar(64) | 可空 | ✅ 存在 | 错误类型 |
| `created_at` | timestamptz | 是 | ✅ 存在 | 创建时间 |

**索引情况**:
- ✅ `ix_agent_node_trace_run_id` (run_id)

---

## 2. 数据写入检查

### 2.1 Harness代码实现

✅ **完整实现** - 文件位置: `backend/src/agent/harness/trace_log.py`

**核心函数**:
1. ✅ `create_agent_run_row()` - 创建run记录
2. ✅ `record_node_trace()` - 记录节点trace
3. ✅ `finalize_agent_run_row()` - 完成run并标记badcase
4. ✅ `resolve_auto_badcase()` - 自动badcase判定逻辑

### 2.2 Decision字段提取

✅ **完整实现** - 文件位置: `backend/src/agent/harness/decision.py`

**各节点decision内容**:

| 节点 | decision字段内容 | 样本数据 | 状态 |
|------|------------------|----------|------|
| **planner** | intent, forced_aggregate, short_circuit, subtask_count | `{"intent": "aggregate", "forced_aggregate": true}` | ✅ 正确 |
| **resolver** | clarify, kind, entities_resolved | 待验证 | ⚠️ 需补充 |
| **retriever** | path, l3_id, rows_returned, pii_masked | `{"path": "structured", "l3_id": "l3-2-5-1", "rows_returned": 2}` | ✅ 正确 |
| **document** | path, chunks_hit, top_score, l3_id | 待验证 | ⚠️ 需补充 |
| **analyst** | skill_used, factors_count, has_baseline | 待验证 | ⚠️ 需补充 |
| **composer** | charts, citations, composed, salary_fields_skipped | `{"charts": 0, "composed": true}` | ⚠️ 缺citations和salary_fields_skipped |
| **critic** | decision, gaps_count | 待验证 | ⚠️ 需补充 |
| **supervisor** | dispatch, subtask_id | `{"dispatch": "compose"}` | ✅ 正确 |

### 2.3 Badcase自动标记

✅ **已实现** - 文件位置: `backend/src/agent/harness/trace_log.py:162-167`

**当前实现的badcase标记**:
1. ✅ `rag_zero_hit` - document节点 chunks_hit=0
2. ✅ `replan_exhausted` - critic节点 pass_with_limit 且 replan_count>=2
3. ✅ `timeout` - outcome=timeout (在finalize时标记)
4. ✅ `clarify` - outcome=clarify (在finalize时标记)
5. ✅ `user_down` - user_feedback=down (由反馈接口写入)

---

## 3. 实际数据验证

### 3.1 数据量统计

```sql
SELECT 
    COUNT(*) as total_runs,
    COUNT(DISTINCT outcome) as distinct_outcomes,
    SUM(CASE WHEN auto_badcase THEN 1 ELSE 0 END) as badcase_count
FROM agent_run;
```

**结果**: 
- 总运行数: 12
- 不同结果数: 1 (全部success)
- Badcase数: 6

### 3.2 样本数据检查

**运行记录样本** (ID: `c1e2f8f6-34f9-4c79-83a7-603c832f89c3`):
- intent: `aggregate`
- outcome: `success`
- replan_count: 0
- node_count: 7
- auto_badcase: `false`
- actual_traces: 7 ✅

**节点trace样本**:
```json
// planner节点
{
  "seq": 1,
  "node": "planner",
  "decision": {
    "intent": "aggregate",
    "subtask_id": "planner",
    "forced_aggregate": true
  }
}

// retriever节点
{
  "seq": 5,
  "node": "retriever",
  "decision": {
    "path": "structured",
    "l3_id": "l3-2-5-1",
    "subtask_id": "t2",
    "rows_returned": 2
  },
  "tools_called": [
    {"name": "get_template"},
    {"name": "query_structured"}
  ]
}

// composer节点
{
  "seq": 7,
  "node": "composer",
  "decision": {
    "charts": 0,
    "composed": true,
    "subtask_id": "composer"
  }
}
```

### 3.3 隐私检查

✅ **隐私保护正确**

检查项:
- ❌ decision中**无**原始query文本
- ❌ decision中**无**姓名、工号等PII
- ❌ decision中**无**薪资金额
- ❌ decision中**无**文档正文
- ❌ decision中**无**数据行具体值
- ✅ 仅有: 哈希、字段名、条数、状态、判定元信息

**样本验证**:
```json
// ✅ 正确：只记录行数和l3_id，无具体数据
{
  "path": "structured",
  "l3_id": "l3-2-5-1",
  "rows_returned": 2
}

// ✅ 正确：只记录命中数，无文档内容
{
  "path": "rag",
  "chunks_hit": 3
}
```

---

## 4. 需要补充的内容

### 4.1 Decision字段补充

#### ⚠️ **resolver节点** 
**当前实现**:
```python
elif node_name == "resolver":
    if result.get("clarify"):
        decision["clarify"] = True
        decision["kind"] = (result.get("clarify") or {}).get("kind") or "unknown"
```

**需要补充**:
```python
elif node_name == "resolver":
    if result.get("clarify"):
        decision["clarify"] = True
        decision["kind"] = (result.get("clarify") or {}).get("kind") or "unknown"
    else:
        decision["resolved"] = True
        # 补充：记录哪些维度被解析了
        entities_resolved = []
        if result.get("employee"):
            entities_resolved.append("employee")
        if result.get("org"):
            entities_resolved.append("org")
        if result.get("time_range"):
            entities_resolved.append("time_range")
        if entities_resolved:
            decision["entities_resolved"] = entities_resolved
```

#### ⚠️ **document节点**
**需要补充**: `top_score` 字段

```python
elif node_name == "document":
    merged_evidence = list(state.get("evidence") or []) + list(result.get("evidence") or [])
    hits = _count_doc_hits(merged_evidence)
    if not hits:
        hits = len(result.get("citations") or [])
    decision["path"] = "rag"
    decision["chunks_hit"] = hits
    # 补充：top_score
    top_score = 0.0
    for block in merged_evidence:
        if block.get("kind") == "documents":
            for hit in block.get("hits") or []:
                if isinstance(hit, dict) and "score" in hit:
                    top_score = max(top_score, float(hit.get("score") or 0))
    if top_score > 0:
        decision["top_score"] = round(top_score, 3)
    # 原有逻辑...
```

#### ⚠️ **analyst节点**
**当前实现**:
```python
elif node_name == "analyst":
    decision["charts"] = len(result.get("charts") or state.get("charts") or [])
    analysis = result.get("analysis") or state.get("analysis") or {}
    if "sufficient" in analysis:
        decision["sufficient"] = bool(analysis.get("sufficient"))
```

**需要补充**:
```python
elif node_name == "analyst":
    # 原有逻辑...
    decision["charts"] = len(result.get("charts") or state.get("charts") or [])
    
    # 补充：skill_used
    active_skills = result.get("active_skills") or state.get("active_skills") or []
    if active_skills:
        skill_id = active_skills[0].get("id") if isinstance(active_skills[0], dict) else None
        if skill_id:
            decision["skill_used"] = skill_id
    
    # 补充：factors_count, has_baseline
    analysis = result.get("analysis") or state.get("analysis") or {}
    factors = analysis.get("factors") or []
    if factors:
        decision["factors_count"] = len(factors)
    if analysis.get("baseline"):
        decision["has_baseline"] = True
```

#### ⚠️ **composer节点**
**需要补充**: `citations` 和 `salary_fields_skipped`

```python
elif node_name == "composer":
    citations = result.get("citations") or state.get("citations") or []
    decision["citations"] = len(citations)  # ✅ 已存在
    decision["charts"] = len(result.get("charts") or state.get("charts") or [])
    
    # 补充：salary_fields_skipped
    salary_skipped = 0
    for cite in citations:
        if isinstance(cite, dict) and cite.get("masked_fields"):
            salary_skipped += len(cite["masked_fields"])
    if salary_skipped > 0:
        decision["salary_fields_skipped"] = salary_skipped
    
    if result.get("final"):
        decision["composed"] = True
```

#### ⚠️ **critic节点**
**需要补充**: `gaps_count` 准确计数

```python
elif node_name == "critic":
    if result.get("needs_replan"):
        decision["decision"] = "replan"
        # 补充：统计gap数量
        gaps = result.get("gaps") or []
        decision["gaps_count"] = len(gaps) if gaps else 1
    elif result.get("limitation"):
        decision["decision"] = "pass_with_limit"
        decision["gaps_count"] = 0
    else:
        decision["decision"] = "pass"
        decision["gaps_count"] = 0
```

### 4.2 文件修改清单

需要修改的文件:
1. ✅ `backend/src/agent/harness/decision.py` - 补充decision提取逻辑
2. ✅ `backend/src/agent/harness/trace_log.py` - 已完整，无需修改

---

## 5. 验证测试计划

### 5.1 制造测试场景

**场景1**: RAG 0命中 
```python
# 问一个知识库中不存在的问题
query = "什么是量子纠缠在人力资源管理中的应用？"
# 预期: document节点 decision.chunks_hit = 0
# 预期: agent_run.auto_badcase = true
# 预期: agent_run.badcase_reason = "rag_zero_hit"
```

**场景2**: 超时
```python
# 设置较短的timeout，问一个复杂问题
timeout = 5  # 5秒
query = "分析过去三年所有部门的离职率趋势..."
# 预期: agent_run.outcome = "timeout"
# 预期: agent_run.auto_badcase = true
# 预期: agent_run.badcase_reason = "timeout"
```

**场景3**: Chitchat
```python
query = "你好"
# 预期: planner节点 decision.short_circuit = true
# 预期: agent_run.outcome = "success"
# 预期: agent_run.auto_badcase = false
```

**场景4**: 重新规划次数>=2
```python
# 问一个需要多次replan的问题
query = "..."  # 导致critic多次返回needs_replan
# 预期: agent_run.replan_count >= 2
# 预期: critic节点 decision.decision = "pass_with_limit"
# 预期: agent_run.auto_badcase = true
# 预期: agent_run.badcase_reason = "replan_exhausted"
```

### 5.2 验证SQL

```sql
-- 1. 检查badcase分布
SELECT 
    badcase_reason,
    COUNT(*) as count,
    AVG(total_ms) as avg_ms,
    AVG(replan_count) as avg_replan
FROM agent_run
WHERE auto_badcase = true
GROUP BY badcase_reason
ORDER BY count DESC;

-- 2. 检查decision字段完整性
SELECT 
    node,
    COUNT(*) as total,
    COUNT(CASE WHEN decision IS NOT NULL THEN 1 END) as has_decision,
    COUNT(CASE WHEN decision IS NULL THEN 1 END) as no_decision
FROM agent_node_trace
GROUP BY node
ORDER BY node;

-- 3. 检查RAG 0命中场景
SELECT 
    r.id,
    r.intent,
    r.outcome,
    r.auto_badcase,
    r.badcase_reason,
    t.decision->>'chunks_hit' as chunks_hit
FROM agent_run r
JOIN agent_node_trace t ON r.id = t.run_id
WHERE t.node = 'document'
  AND (t.decision->>'chunks_hit')::int = 0
LIMIT 10;

-- 4. 检查隐私保护
-- 确保decision中无敏感信息
SELECT 
    node,
    decision
FROM agent_node_trace
WHERE decision::text LIKE '%工资%'
   OR decision::text LIKE '%薪资%'
   OR decision::text LIKE '%姓名%'
   OR decision::text LIKE '%员工%'
LIMIT 10;
-- 预期: 0 rows (不应该有匹配)
```

---

## 6. 总结

### 6.1 当前状态

✅ **表结构完整** - 所有必需字段已存在  
✅ **核心逻辑实现** - Harness写入、Badcase标记已完整  
✅ **隐私保护正确** - decision中无敏感信息  
⚠️ **Decision内容需补充** - 部分节点的decision字段不够完整

### 6.2 优先级排序

**P0 (必须补充)**:
1. ✅ resolver节点: `entities_resolved` 字段
2. ✅ document节点: `top_score` 字段
3. ✅ composer节点: `salary_fields_skipped` 字段
4. ✅ critic节点: 准确的 `gaps_count`

**P1 (建议补充)**:
1. analyst节点: `skill_used`, `factors_count`, `has_baseline`

**P2 (可选优化)**:
1. 添加更多badcase场景监测
2. 优化decision sanitize逻辑

### 6.3 下一步行动

1. ✅ 修改 `backend/src/agent/harness/decision.py` 补充decision字段
2. ⏸ 运行测试场景验证
3. ⏸ 执行验证SQL确认数据正确性
4. ⏸ 更新文档

---

**自查完成时间**: 2026-05-28 01:15 AM  
**复盘Agent可用性**: ✅ 基本可用，建议补充P0字段后正式使用
